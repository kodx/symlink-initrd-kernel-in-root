# Symlink latest kernel and initramfs in root

Create symlinks to the latest kernel and initramfs in the root, debian-like (/vmlinuz and /initrd.img). Update symlinks on every kernel install or update.
